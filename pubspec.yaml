name: tradeable_learn_widget
description: "A new Flutter package project."
publish_to: "none"
version: 0.0.1
homepage:

environment:
  sdk: ^3.5.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  auto_size_text: ^3.0.0
  intl: ^0.20.2
  youtube_player_flutter: ^9.1.0
  flutter_markdown: ^0.7.7+1
  smooth_page_indicator: ^1.2.0+3
  syncfusion_flutter_gauges: ^30.2.6
  dart_eval: ^0.8.1
  fl_chart: ^1.0.0
  flutter_heatmap_calendar: ^1.0.5
  carousel_slider_plus: ^7.0.2
  lottie: ^3.3.1
  webview_flutter: ^4.10.0
  photo_view: ^0.15.0
  fin_chart:
    git:
      url: https://github.com/adityagohad/fin_chart.git
      ref: dev
  file_picker: ^10.0.0
  dio: ^5.8.0+1
  aws_s3_upload_lite: ^0.1.6
  path: ^1.9.1
  http: ^1.3.0
  crypto: ^3.0.6
  url_launcher: ^6.3.2
  flutter_svg: ^2.2.0
  markdown_widget: ^2.3.2+8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # To add assets to your package, add an assets section, like this:
  assets:
    - lib/assets/
    - lib/assets/risk_reward_ratio/
    - lib/assets/market_depth/
    - lib/assets/supply_demand_educorner/
    - lib/assets/lottie/
  #
  # For details regarding assets in packages, see
  # https://flutter.dev/to/asset-from-package
  #
  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # To add custom fonts to your package, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts in packages, see
  # https://flutter.dev/to/font-from-package
